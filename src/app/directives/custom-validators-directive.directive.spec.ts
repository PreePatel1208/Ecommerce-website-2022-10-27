import { CustomValidatorsDirectiveDirective } from './custom-validators-directive.directive';

describe('CustomValidatorsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomValidatorsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
